# v0.3.5 release notes

- **Club website on Home.** A new card under "Do a thing" opens
  boardgamenightwg.com in the coordinator's browser — the same external
  ACTION_VIEW handoff as the Google Groups members deep link. The app never
  embeds or writes to the site; a failed handoff says so on the card.

## Release metadata and gates

- App version: `0.3.5`; Tauri derives Android `versionCode` **3005**.
- Updater contract: tag `v0.3.5`, asset `bgn-coordinator_0.3.5_arm64.apk`.
- Latest published version checked before this bump: `v0.3.4`.
- **Do not merge/release without owner approval**: merging triggers the
  signed arm64 publication workflow.
- DOM verification (node:test + linkedom) covers the card's presence and the
  handoff URL; the on-phone tap → external browser behavior rides the same
  `tauri-plugin-opener` path already proven by the members-page deep link.
